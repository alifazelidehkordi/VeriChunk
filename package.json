{
  "name": "doc-splitter-mcp",
  "version": "0.5.0",
  "description": "MCP server for VeriChunk verified semantic document chunking",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^3.23.0"
  }
}
